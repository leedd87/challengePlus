import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    marginBottom: 20,
    marginHorizontal: 25,
    padding: 10,
    gap: 10,
    borderWidth: 1,
    borderRadius: 10,
  },
  text: {
    fontWeight: 'bold',
    fontSize: 20,
  },
});
